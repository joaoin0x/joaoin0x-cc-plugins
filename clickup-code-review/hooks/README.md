# Hook Templates

This directory contains hook **templates**, not auto-discovered hooks.

`clickup-auto-approve.sh` is installed by `/clickup-code-review:setup` (Step 6) into the user's settings directory with the correct `SETTINGS_PATH` substituted. It is NOT loaded directly from this location.
